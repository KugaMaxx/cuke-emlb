register_module(double_window_filter)
register_module(event_flow)
register_module(khodamoradi_noise)
register_module(reclusive_event_denoisor)
register_module(time_surface)
register_module(yang_noise)

# if(EMLB_ENABLE_TORCH)
    # register_module(event_denoise_convolution_network)
    # register_module(multi_layer_perceptron_filter)
# endif()
